= form_for @model, :html => { :class => 'form-horizontal' } do |f|
  .form-group
    = f.label :brand_id, :class => 'control-label'
    .controls
      = f.select :brand_id,Brand.all.map{|b| [b.name,b.id]}, :class => 'form-control'
  .form-group
    = f.label :name, :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'form-control'
  .form-group
    = f.label :color, :class => 'control-label'
    .controls
      = f.select :color,Colour.all.map{|b| [b.name,b.name]}, :class => 'form-control'
  .form-group
    = f.label :where_serial_number_is_found, :class => 'control-label'
    .controls
      = f.text_field :serial_no_location, :class => 'form-control'
  .form-group
    = f.label "warranty (in months)", :class => 'control-label'
    .controls
      = f.text_field :warranty, :class => 'form-control'
  .form-group
    = f.label :image, :class => 'control-label'
    .controls
      = f.file_field :image, :class => ''
      
  = f.submit nil, :class => 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), models_path, :class => 'btn btn-default'
